import React from "react";
import { withRouter } from "react-router-dom";

const CreateOrder: React.FunctionComponent = () => {
  return <div>Create Order Works!</div>;
};

export default withRouter(CreateOrder);
